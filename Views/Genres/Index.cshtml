@model List<Genre>

@{
    ViewData["Title"] = "Genres";
    ViewData["BodyClass"] = "page-mario page-genres";
}

<section class="mario-stage mario-stage-prose">
    <div class="mario-panel mario-panel-wide mario-panel-split">
        <div class="mario-panel-header">
            <div>
                <h1>@ViewData["Title"]</h1>
            </div>
            <a asp-action="Create" class="btn btn-sm">New Genre</a>
        </div>

        @if (TempData["Success"] != null)
        {
            <div class="login-alert home-alert login-alert-success" role="alert">@TempData["Success"]</div>
        }
        @if (TempData["Error"] != null)
        {
            <div class="login-alert home-alert login-alert-error" role="alert">@TempData["Error"]</div>
        }

        <div class="mario-panel-card">
            <div class="mario-search">
                <label for="genre-search" class="form-label">Search genres</label>
                <div class="mario-search-row">
                    <input id="genre-search" type="text" class="form-control mario-input js-list-search" placeholder="Search by name or description..." data-search-url="@Url.Action("Search", "Genres")" data-target="#genre-list" />
                    <span class="mario-search-spinner" aria-hidden="true"></span>
                </div>
            </div>

            <div id="genre-list" class="mario-list-region">
                @await Html.PartialAsync("_GenreTable", Model)
            </div>
        </div>
    </div>
</section>
