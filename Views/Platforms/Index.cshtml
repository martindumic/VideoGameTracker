@model List<Platform>

@{
    ViewData["Title"] = "Platforms";
    ViewData["BodyClass"] = "page-mario page-platforms";
}

<section class="mario-stage mario-stage-prose">
    <div class="mario-panel mario-panel-wide mario-panel-split">
        <div class="mario-panel-header">
            <div>
                <h1>@ViewData["Title"]</h1>
            </div>
            @if (User.IsInRole("Admin"))
            {
                <a asp-action="Create" class="btn btn-sm">New Platform</a>
            }
        </div>

        @if (TempData["Success"] != null)
        {
            <div class="login-alert home-alert login-alert-success" role="alert">@TempData["Success"]</div>
        }
        @if (TempData["Error"] != null)
        {
            <div class="login-alert home-alert login-alert-error" role="alert">@TempData["Error"]</div>
        }

        <div class="mario-panel-card">
            <div class="mario-search">
                <label for="platform-search" class="form-label">Search platforms</label>
                <div class="mario-search-row">
                    <input id="platform-search" type="text" class="form-control mario-input js-list-search" placeholder="Search platforms..." data-search-url="@Url.Action("Search", "Platforms")" data-target="#platform-list" />
                    <span class="mario-search-spinner" aria-hidden="true"></span>
                </div>
            </div>

            <div id="platform-list" class="mario-list-region">
                @await Html.PartialAsync("_PlatformTable", Model)
            </div>
        </div>
    </div>
</section>
